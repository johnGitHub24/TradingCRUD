spring:
  datasource:
    url: jdbc:h2:mem:tradingcrud-test;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    username: sa
    password: ""
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false

app:
  jwt:
    secret: test-secret-key-for-junit-tests-only-min-32-bytes-long
    expiration-minutes: 60
    issuer: trading-crud-test
  admin:
    username: admin
    password: admin123
